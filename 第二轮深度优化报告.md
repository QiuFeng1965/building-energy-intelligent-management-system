# 第二轮深度优化报告

> 生成时间：2026-07-28
> 优化范围：擎翼数字中枢全栈系统
> 验证状态：✅ 全部通过（28 项 pytest + 接口实测）

---

## 一、优化项总览

| 序号 | 优化项 | 优先级 | 状态 | 影响文件 |
|------|--------|--------|------|----------|
| 1 | Alembic 数据库迁移工具引入 | 高 | ✅ 完成 | alembic.ini, env.py, versions/0001_baseline.py, versions/0002_add_indexes.py |
| 2 | 数据库性能索引补全 | 高 | ✅ 完成 | versions/0002_add_indexes.py |
| 3 | 结构化 JSON 日志 + trace_id 链路追踪 | 高 | ✅ 完成 | app/core/logging_config.py, app/core/middleware.py, app/main.py |
| 4 | Prophet 预测模型优化（缓存+异步+限流+降级） | 中 | ✅ 完成 | app/api/v1/energy.py |
| 5 | admin.py 功能补全（真实 KPI + 知识库 CRUD + 审计日志） | 高 | ✅ 完成 | app/api/v1/admin.py |
| 6 | 前端 ESLint + Prettier + Vitest 配置 | 中 | ✅ 完成 | frontend/eslint.config.js, .prettierrc.json, vitest.config.js, package.json |
| 7 | ai_service.py control_device 列名修复 | 高 | ✅ 完成 | app/services/ai_service.py |

---

## 二、详细变更说明

### 1. Alembic 数据库迁移工具

**目标**：引入版本化数据库 schema 管理，支持增量迁移和回滚。

**变更**：
- 配置 `alembic.ini` 和 `alembic/env.py`，从项目配置中心读取数据库路径
- 创建 `0001_baseline.py` 基线迁移（标记现有 schema 为已初始化状态）
- 创建 `0002_add_indexes.py` 索引迁移（7 个性能索引）
- 支持 SQLite（开发）和 PostgreSQL（生产）双模式
- 启用 `render_as_batch=True` 支持 SQLite 的 ALTER TABLE 操作

**验证**：`alembic upgrade head` 执行成功，alembic_version 表已创建。

---

### 2. 数据库性能索引补全

**目标**：为 110 万行能耗表的常用查询路径添加索引，提升 Dashboard/报表/设备监控的查询速度。

**新增索引**（7 个）：

| 索引名 | 表 | 列 | 查询场景 |
|--------|----|----|----------|
| idx_energy_date | fact_energy_records | DATE(monitor_time) | 按日期范围查询（Dashboard 今日数据、报表周报） |
| idx_energy_device_time | fact_energy_records | device_id, monitor_time | 按设备+时间查询（设备监控、AI 工具函数） |
| idx_energy_building_time | fact_energy_records | building_id, monitor_time | 按建筑+时间查询（空间孪生、报表分建筑统计） |
| idx_energy_status | fact_energy_records | run_status, DATE(monitor_time) | 按运行状态查询（异常告警筛选） |
| idx_workorders_status | fact_work_orders | status, created_at | 按状态查询工单 |
| idx_workorders_device | fact_work_orders | device_id, created_at | 按设备查询工单 |
| idx_agent_memory_incident | sys_agent_memory | incident_id, created_at | 按事件查询智能体记忆 |

**修复**：迁移脚本初始版本使用了错误的列名（`created_time` → `created_at`，`session_id` → `incident_id`），已根据实际数据库 schema 修正。

---

### 3. 结构化 JSON 日志 + trace_id 链路追踪

**目标**：所有日志输出为 JSON 格式，每条日志携带 trace_id，便于 ELK/Loki 采集和全链路追踪。

**新增文件**：
- `app/core/logging_config.py`：JsonFormatter + 按天轮转文件日志 + ContextVar trace_id
- `app/core/middleware.py`：TraceMiddleware（自动注入 trace_id + 用户上下文 + 请求计时 + 访问日志）

**变更**：
- `app/main.py`：用 `setup_logging()` 替代 `logging.basicConfig`，注册 TraceMiddleware
- CORS 头部新增 `X-Trace-Id` 允许列表
- 异常处理器改用结构化日志（`extra={...}` 字段）

**日志格式示例**：
```json
{
  "ts": "2026-07-28T04:55:01.123+00:00",
  "level": "INFO",
  "logger": "app.core.middleware",
  "msg": "请求完成",
  "trace_id": "e2fe006e10c94cdf",
  "user": "admin",
  "method": "GET",
  "path": "/api/admin/dashboard",
  "status": 200,
  "elapsed_ms": 23.45
}
```

**验证**：响应头返回 `X-Trace-Id: e2fe006e10c94cdf`，日志文件输出到 `backend/logs/app.jsonl`。

---

### 4. Prophet 预测模型优化

**目标**：从随机 mock 数据升级为真实数据库数据 + 模型缓存 + 异步训练 + 限流 + 降级标记。

**优化项**：
1. **真实数据源**：从 `fact_energy_records` 按小时聚合真实能耗数据（SUM + AVG），替代随机数生成
2. **模型缓存**：TTL 1 小时，按小时粒度生成缓存 key，命中缓存时跳过 Prophet 训练
3. **异步训练**：`asyncio.to_thread(_train_prophet_model)` 避免阻塞事件循环
4. **限流**：`@limiter.limit("10/minute")` 防止预测接口被滥用
5. **三级降级**：
   - 数据库有数据 → Prophet 训练（`data_source: real_db`）
   - 数据库无数据 → 规则模型生成历史（`data_source: rule_based_degraded`）
   - Prophet 训练失败 → 规则模型预测（`data_source: rule_fallback: {error}`）
6. **缓存清理**：自动清理过期缓存（TTL × 2），防止内存泄漏

**响应新增字段**：
```json
{
  "meta": {
    "data_source": "real_db",
    "model_status": "freshly_trained",
    "cache_ttl_seconds": 3600
  }
}
```

---

### 5. admin.py 功能补全

**目标**：从硬编码 KPI 升级为真实数据库聚合 + 知识库 CRUD + 审计日志体系。

**新增端点**（5 个）：

| 端点 | 方法 | 功能 |
|------|------|------|
| /api/admin/dashboard | GET | 真实 KPI 聚合（设备数、能耗、告警、异常记录） |
| /api/admin/audit_logs | GET | 审计日志查询（支持 risk_level 过滤） |
| /api/admin/audit_logs | POST | 手动写入审计日志 |
| /api/admin/kb/list | GET | 知识库文档列表 |
| /api/admin/kb/upload | POST | 知识库文档录入（有界队列） |
| /api/admin/kb/{doc_index} | DELETE | 知识库文档删除 |

**新增基础设施**：
- `sys_audit_logs` 审计日志表（自动创建，幂等）
- `write_audit_log()` 公共函数（供其他模块调用，自动注入 trace_id）
- 真实 KPI 聚合函数（今日能耗、异常数、设备数、告警数）

**验证结果**（真实数据）：
```
KPI:
  ai_messages: 1,112,520（累计能耗记录数）
  abnormal_visits: 207（异常记录数）
  total_devices: 127（设备总数）
  total_energy_kwh: 45,428.65 kWh（今日总能耗）
  today_alarms: 0（今日告警数）
```

---

### 6. 前端 ESLint + Prettier + Vitest 配置

**目标**：为前端项目配置代码质量工具链。

**新增文件**：
- `frontend/eslint.config.js`：ESLint 9 flat config（vue3-recommended + Prettier 兼容）
- `frontend/.prettierrc.json`：Prettier 规则（semi: false, singleQuote: true）
- `frontend/vitest.config.js`：Vitest 配置（jsdom + @vue/test-utils）
- `frontend/.eslintignore` / `.prettierignore`：忽略配置
- `frontend/src/utils/__tests__/request.test.js`：单元测试示例（7 个测试用例）

**新增 npm scripts**：
- `npm run lint`：ESint 检查 + 自动修复
- `npm run format`：Prettier 格式化
- `npm test`：Vitest 单元测试
- `npm run test:watch`：Vitest 监听模式

**注意**：需运行 `npm install` 安装新增 devDependencies。

---

### 7. ai_service.py control_device 列名修复

**问题**：`control_device` 函数的 INSERT 语句使用了不存在的列名（`building_id`, `action`, `created_time`）。

**修复**：
- `created_time` → `created_at`
- 移除不存在的 `building_id` 列
- `action` → 映射到 `maintenance_action` 列
- 新增 `anomaly_time` 和 `diagnosis_title` 列填充
- 新增审计日志记录（`risk_level: high`）

---

## 三、验证结果

### 3.1 pytest 测试
```
============================= 28 passed in 5.60s ==============================
```
- TestLogin: 4 项通过
- TestDashboard: 3 项通过
- TestDevices: 3 项通过
- TestSpatialTwin: 4 项通过
- TestAuthProtection: 3 项通过
- test_sql_sandbox: 11 项通过

### 3.2 接口实测

| 接口 | 状态 | 结果 |
|------|------|------|
| GET /health | 200 | `{"status": "ok"}` + X-Trace-Id 响应头 |
| GET /readiness | 200 | `{"db": "ok", "core_tables": "ok(rows=1112520)"}` |
| POST /api/login | 200 | 返回 JWT token |
| GET /api/admin/dashboard | 200 | 真实 KPI（127 设备、111 万记录、45428 kWh） |
| POST /api/admin/kb/upload | 200 | 知识库录入成功（KB-1） |
| GET /api/admin/audit_logs | 200 | 审计日志含 trace_id |
| alembic upgrade head | 成功 | 7 个索引已创建 |

### 3.3 Alembic 迁移
```
INFO [alembic.runtime.migration] Running upgrade  -> 0001_baseline
INFO [alembic.runtime.migration] Running upgrade 0001_baseline -> 0002_add_indexes
```

---

## 四、文件变更清单

### 新增文件（8 个）
1. `backend/alembic/versions/0001_baseline.py` - Alembic 基线迁移
2. `backend/alembic/versions/0002_add_indexes.py` - 性能索引迁移
3. `backend/app/core/logging_config.py` - 结构化 JSON 日志配置
4. `backend/app/core/middleware.py` - TraceMiddleware 链路追踪中间件
5. `frontend/eslint.config.js` - ESLint 9 flat config
6. `frontend/.prettierrc.json` - Prettier 规则
7. `frontend/vitest.config.js` - Vitest 配置
8. `frontend/src/utils/__tests__/request.test.js` - 单元测试示例

### 修改文件（5 个）
1. `backend/app/main.py` - 集成结构化日志 + TraceMiddleware
2. `backend/app/api/v1/energy.py` - Prophet 优化（缓存+异步+限流+降级+真实数据）
3. `backend/app/api/v1/admin.py` - 功能补全（真实 KPI + 知识库 CRUD + 审计日志）
4. `backend/app/services/ai_service.py` - control_device 列名修复 + 审计日志
5. `frontend/package.json` - 新增 ESLint/Prettier/Vitest 依赖和脚本

---

## 五、后续可选优化方向

1. **前端接入新 API**：AdminDashboard.vue 目前大部分 KPI 仍为硬编码，可接入 `/api/admin/dashboard` 和 `/api/admin/audit_logs` 获取真实数据
2. **Prophet 模型持久化**：将训练好的模型序列化到磁盘，避免重启后重新训练
3. **日志采集管道**：部署 ELK/Loki 采集 `app.jsonl` 日志，实现可视化链路追踪
4. **PostgreSQL 生产切换**：通过 `DATABASE_URL` 环境变量切换到 PostgreSQL，Alembic 迁移已支持
5. **前端单元测试覆盖**：扩展 Vitest 测试用例，覆盖 Vue 组件和 API 调用
6. **审计日志自动记录**：在 SQL 沙箱拦截、AI 高危操作等场景自动调用 `write_audit_log()`
