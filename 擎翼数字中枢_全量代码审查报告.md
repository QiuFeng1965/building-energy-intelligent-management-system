# 擎翼数字中枢 — 全量代码审查报告

> **审查日期**：2026-07-28
> **审查范围**：后端代码质量 / 前端代码质量 / 数据库层 / 安全性 / 性能 / 测试覆盖 / 部署配置
> **审查方式**：静态代码审查，未修改任何代码
> **项目路径**：`c:\Users\Administrator\Desktop\🔧 开发项目\Building Energy Intelligent Management System2`

---

## 📊 审查概览

| 维度 | 严重问题 | 中等问题 | 优化建议 |
|------|---------|---------|---------|
| 后端代码质量 | 5 | 8 | 4 |
| 前端代码质量 | 0 | 5 | 3 |
| 数据库层 | 1 | 2 | 2 |
| 安全性 | 4 | 3 | 2 |
| 性能 | 0 | 3 | 3 |
| 测试覆盖 | 0 | 3 | 2 |
| 部署配置 | 1 | 3 | 2 |
| **合计** | **11** | **27** | **18** |

---

# 🔴 严重问题（必须修复）

## 【严重-01】上传文档上下文永久失效 — `knowledge_base` 全局检查逻辑 Bug

- **问题描述**：在 AI 对话流式生成器中，使用 `if 'knowledge_base' in globals() and knowledge_base:` 检查全局变量。但 `knowledge_base` 是通过 `from app.main import knowledge_base` 导入到函数局部作用域的，永远存在于 `locals()` 而非 `globals()`。该条件恒为 `False`，导致用户上传的文档内容**永远不会**被注入到 AI 系统提示词中，核心功能静默失效。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 265 行
  ```python
  if 'knowledge_base' in globals() and knowledge_base:
  ```
- **建议修复**：改为 `if knowledge_base:` 即可（导入后变量必然存在于局部作用域）。

---

## 【严重-02】dashboard.py 数据库异常时静默返回硬编码 Mock 数据

- **问题描述**：`/api/dashboard` 接口在数据库读取异常时，`print` 一条日志后返回一套写死的假数据（总耗电 12,450 kWh、COP 4.2、3 个告警等）。这会掩盖真实的数据库故障，让前端展示"看似正常"的假数据，用户和管理员无法察觉系统已宕机，违反"数据真实性"铁律。
- **涉及文件**：`backend/app/api/v1/dashboard.py`
- **代码位置**：第 101-119 行
  ```python
  except Exception as e:
      print(f"🚨 数据库读取失败，已启用 Mock 兜底数据。错误原因: {e}")
      return {
          "kpi": {"total_elec": "12,450", "carbon": "9,773", "cop": "4.2", "alarms": "3"},
          ...
      }
  ```
- **建议修复**：直接抛出 `DBUnavailableError`（由全局异常处理器返回 503），或返回 `{"status": "error", "code": "DB_UNAVAILABLE"}` 让前端展示错误态。项目已有 `SkeletonLoader` / `ErrorState` 组件可承接。

---

## 【严重-03】`/api/cop_trend` 接口完全是伪造的随机数据

- **问题描述**：该接口使用 `math.sin` + `random.uniform` 生成 24 小时 COP 趋势，**从不查询数据库**。前端仪表盘上展示的"COP 趋势曲线"是纯粹的假数据，与真实设备运行状态无关。这属于功能性欺骗，在工业能源管理系统中是不可接受的。
- **涉及文件**：`backend/app/api/v1/dashboard.py`
- **代码位置**：第 122-148 行
  ```python
  for i in range(24):
      ...
      base_cop = 4.5
      variation = math.sin((t.hour / 24) * math.pi * 2) * 0.8
      random_noise = random.uniform(-0.2, 0.2)
      values.append(round(base_cop + variation + random_noise, 2))
  ```
- **建议修复**：改为从 `fact_energy_records` 按小时聚合 `AVG(cop)`，参考 `/api/dashboard` 中 `cop_df` 的写法（第 47-52 行已有正确示例）。

---

## 【严重-04】`/api/equipment/predictive_maintenance` 接口完全是随机伪造数据

- **问题描述**：该接口声称提供"预测性维护 (RUL)"数据，但实际只用 `random.uniform(2.5, 6.8)` 和 `random.uniform(65, 95)` 生成振动和温度值，再用一个简单公式算出"健康度"和"剩余寿命"。**完全不调用**项目已训练的 `rul_prediction_model.pkl` 模型，也不查数据库。前端展示的"高危预警"完全是随机的。
- **涉及文件**：`backend/app/api/v1/devices.py`
- **代码位置**：第 104-138 行
  ```python
  vibration = random.uniform(2.5, 6.8)
  temp = random.uniform(65, 95)
  health_score = 100 - ((vibration - 2.0) * 8) - ((temp - 60) * 0.5)
  ```
- **建议修复**：参考 `chat.py` 第 454-489 行 `predict_device_rul` 工具的实现，加载 `MODEL_PATH` 下的 `.pkl` 模型，从数据库取真实传感器特征进行推理。

---

## 【严重-05】`predict_device_rul` 工具对非 EVC 设备使用随机特征

- **问题描述**：在 AI 工具调用中，`predict_device_rul` 对"充电桩/EVC"类设备使用硬编码特征值（vibration_rms=8.5 等），对其他所有设备则用 `random.uniform` 生成随机特征。这意味着用户询问任何非充电桩设备的寿命预测时，得到的都是基于随机数的"AI 预测结果"，且没有标注 `data_source` 标志，用户无法区分真实 vs 模拟。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 466-487 行
  ```python
  if any(target in device_id.upper() for target in target_devices):
      raw_data = {'vibration_rms': [8.5], 'temp_offset': [13.2], 'current_fluctuation': [9.1]}
  else:
      raw_data = {
          'vibration_rms': [random.uniform(1.5, 3.5)],
          'temp_offset': [random.uniform(0.5, 2.0)],
          'current_fluctuation': [random.uniform(1.0, 3.0)]
      }
  ```
- **建议修复**：从 `fact_energy_records` 或专用传感器表读取真实特征；若无真实数据，必须在返回结果中明确标注 `【模拟数据 - 仅供参考】`。

---

## 【严重-06】文件上传接口无类型/大小校验，存在 DoS 与安全风险

- **问题描述**：`/api/upload_doc` 接口直接 `await file.read()` 读取整个文件到内存，没有：① 文件大小限制；② 文件类型白名单校验（MIME / 扩展名）；③ 文件内容嗅探。攻击者可上传超大文件导致内存耗尽（OOM DoS），或上传可执行脚本通过社工手段触发。`decode('utf-8')` 也会在二进制文件上抛异常但被 except 兜住，仅返回模糊错误。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 536-554 行
  ```python
  content_bytes = await file.read()
  content_text = content_bytes.decode('utf-8')
  ```
- **建议修复**：
  1. 限制 `MAX_FILE_SIZE = 5 * 1024 * 1024`（5MB），读取前用 `file.size` 校验；
  2. 校验 `file.content_type` 白名单（`text/plain, application/pdf, application/vnd.openxmlformats-officedocument.wordprocessingml.document`）；
  3. 校验扩展名白名单（`.txt, .pdf, .docx, .md`）。

---

## 【严重-07】全局 `knowledge_base` 队列跨用户共享，存在隐私泄露与并发竞态

- **问题描述**：`app/main.py` 中 `knowledge_base = deque(maxlen=50)` 是进程级全局变量，所有用户、所有会话共享。A 用户上传的文档会被 B 用户的 AI 对话读取到，存在隐私泄露。同时 `admin.py` 的删除接口（第 316-351 行）执行 `clear() + extend()` 非原子操作，并发请求可能导致数据丢失或索引错位。
- **涉及文件**：`backend/app/main.py` 第 47 行；`backend/app/api/v1/admin.py` 第 316-351 行
- **建议修复**：改为按 `user` 或 `session_id` 隔离的 `dict[str, deque]`；或迁移到数据库表 `sys_knowledge_base`，利用 SQLite 行级锁解决并发。

---

## 【严重-08】默认管理员密码哈希硬编码在配置中心

- **问题描述**：`config.py` 中 `ADMIN_PASSWORD_HASH` 提供了一个默认的 bcrypt 哈希值。虽然 `.env.example` 提示需要替换，但如果部署时遗忘配置 `.env`，该默认哈希将生效。该哈希对应的明文密码未知，但存在被逆向破解的风险。同时该默认值看起来像真实哈希，迷惑性强。
- **涉及文件**：`backend/app/core/config.py`
- **代码位置**：第 65-68 行
  ```python
  ADMIN_PASSWORD_HASH = os.environ.get(
      "ADMIN_PASSWORD_HASH",
      "$2b$12$LJ3m4ys3Lz0QFDnKDv0xvO0xgQY0m0q3qXv5eLZ5k1Y8m9n2oPqW"
  )
  ```
- **建议修复**：参照 `JWT_SECRET` 的强制校验逻辑，缺失时直接 `raise RuntimeError`，拒绝启动。即：`if not ADMIN_PASSWORD_HASH: raise RuntimeError("ADMIN_PASSWORD_HASH 必须在 .env 中设置")`。

---

## 【严重-09】RagFlow 服务用 `print` 替代 logger，且 prompt 注入式"传话筒"包装存在风险

- **问题描述**：`ragflow_service.py` 中大量使用 `print()` 打印调试信息（第 41、51、59、64、80、83、85 行），这些输出不会被 JSON 日志系统采集，无法进入 ELK/Loki，违反结构化日志规范。更严重的是第 88-98 行的 `magic_wrapper` 将知识库原文包裹在"系统最高优先级指令"中要求 LLM 一字不改输出，这构成了**prompt injection 攻击向量**——若知识库文档被恶意编辑（含 `ignore previous instructions` 等内容），LLM 会无条件执行。
- **涉及文件**：`backend/app/services/ragflow_service.py`
- **代码位置**：第 41、51、59、64、80、83、85 行（print）；第 88-98 行（magic_wrapper）
- **建议修复**：
  1. 全部替换为 `logger.info()` / `logger.warning()`；
  2. 取消"传话筒"强制指令，改为中性描述："以下是知识库检索结果，请综合参考："；
  3. 对知识库原文做 prompt injection 检测（扫描 `ignore previous`、`system:` 等关键词）。

---

## 【严重-10】docker-compose 默认 PostgreSQL 密码硬编码

- **问题描述**：`docker-compose.yml` 中 `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-qingyi_prod_2026}`，若环境变量未设置，则使用弱默认密码 `qingyi_prod_2026`。该密码已提交到代码库，公网扫描器可轻易爆破。同时 `backend` 服务的 `DATABASE_URL` 也使用同一默认密码。
- **涉及文件**：`docker-compose.yml`
- **代码位置**：第 15 行、第 42 行
  ```yaml
  POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-qingyi_prod_2026}
  ```
- **建议修复**：移除默认值，强制要求设置：`POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?必须设置 POSTGRES_PASSWORD 环境变量}`。

---

## 【严重-11】后端 Docker 容器以 root 用户运行

- **问题描述**：`backend/Dockerfile` 没有创建非 root 用户，容器内进程以 root 身份运行。一旦应用存在 RCE 漏洞（如 AI 工具调用的 `eval`、文件上传漏洞），攻击者直接获得容器 root 权限，可逃逸到宿主机。
- **涉及文件**：`backend/Dockerfile`
- **代码位置**：整个文件未出现 `USER` 指令
- **建议修复**：
  ```dockerfile
  RUN useradd -m -u 1000 appuser
  USER appuser
  ```

---

# 🟡 中等问题（建议修复）

## 【中等-01】chat.py 大量使用 `print()` 替代 logger

- **问题描述**：AI 对话路由中超过 8 处使用 `print()` 打印调试信息（第 401、426、447、456、491、508、524、528 行），这些输出绕过了结构化 JSON 日志系统，无法携带 `trace_id`，无法被日志采集系统消费，也无法按级别过滤。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 401、426、447、456、491、508、524、528 行
- **建议修复**：全部替换为 `logger.info()` / `logger.warning()`，并利用 `extra={}` 携带结构化字段。

---

## 【中等-02】main.py、dashboard.py、spatial_twin.py 中残留 `print()`

- **问题描述**：`main.py` 第 63 行 `print("⏰ 定时邮件推送服务已启动...")`；`dashboard.py` 第 102、189 行；`spatial_twin.py` 第 70 行均使用 `print`。与项目已配置的 JSON 日志系统不一致。
- **涉及文件**：`backend/app/main.py`、`backend/app/api/v1/dashboard.py`、`backend/app/api/v1/spatial_twin.py`
- **建议修复**：统一替换为 `logger.info()` / `logger.exception()`。

---

## 【中等-03】chat.py 中 `MAX_RETRIES = 5` 过高，单次对话可能耗时数分钟

- **问题描述**：AI 对话最大重试 5 轮，每轮 LLM 调用 + 工具执行可能耗时 30 秒以上，最坏情况下用户等待 2.5 分钟才能收到首字响应。同时 `max_tokens=16384`（文本模型）单次调用成本极高。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 398、409 行
- **建议修复**：`MAX_RETRIES` 降至 3；`max_tokens` 降至 4096；增加单次请求总超时（如 60 秒）。

---

## 【中等-04】AsyncOpenAI 客户端未设置超时，可能无限挂起

- **问题描述**：`ai_service.py` 第 32-35 行初始化 `AsyncOpenAI` 时未传入 `timeout` 参数，虽然 `config.py` 定义了 `LLM_TIMEOUT=30.0` 和 `LLM_TOTAL_TIMEOUT=120`，但从未被使用。若 LLM 服务无响应，请求将无限挂起，最终前端 SSE 连接超时但后端协程泄漏。
- **涉及文件**：`backend/app/services/ai_service.py`
- **代码位置**：第 32-35 行
  ```python
  ai_client = AsyncOpenAI(
      api_key=AI_API_KEY,
      base_url=AI_BASE_URL
  )
  ```
- **建议修复**：`ai_client = AsyncOpenAI(api_key=AI_API_KEY, base_url=AI_BASE_URL, timeout=LLM_TIMEOUT)`。

---

## 【中等-05】AI 工具调用中 `func_args = json.loads(tool_call.function.arguments)` 缺乏容错

- **问题描述**：LLM 返回的 `function.arguments` 是 JSON 字符串，但偶尔会返回不合法的 JSON（尤其在低温模型或长上下文时）。直接 `json.loads` 会抛 `JSONDecodeError`，导致整个工具调用循环中断，用户收到"系统大脑连接失败"错误。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 425 行
- **建议修复**：
  ```python
  try:
      func_args = json.loads(tool_call.function.arguments)
  except json.JSONDecodeError:
      func_args = {}
      # 跳过该工具调用并通知模型
  ```

---

## 【中等-06】前端 `isLoggedIn` 状态非响应式，token 过期后无法自动登出

- **问题描述**：`App.vue` 第 199 行 `const isLoggedIn = ref(!!localStorage.getItem('token'))` 仅在初始化时读取一次。JWT token 过期后（8 小时），用户仍能浏览界面，直到发起 API 请求被 401 拦截才触发 `window.location.reload()`，体验生硬。
- **涉及文件**：`frontend/src/App.vue`
- **代码位置**：第 199 行
- **建议修复**：在 `request.js` 的 401 拦截器中通过事件总线或 Pinia store 主动更新 `isLoggedIn`，而非 `window.location.reload()`。

---

## 【中等-07】前端路由守卫仅检查 token 存在性，不校验过期时间

- **问题描述**：`router/index.js` 第 62-74 行的 `beforeEach` 守卫只判断 `localStorage.getItem('token')` 是否存在，不解析 JWT 的 `exp` 字段。过期 token 仍能通过守卫进入受保护页面，直到第一次 API 调用才被发现。
- **涉及文件**：`frontend/src/router/index.js`
- **代码位置**：第 62-74 行
- **建议修复**：解析 JWT payload（base64 decode），检查 `exp > Date.now()/1000`；或依赖 `request.js` 的 401 拦截主动登出。

---

## 【中等-08】request.js 在 401 时执行 `window.location.reload()`，UX 粗暴

- **问题描述**：`safeFetch` 检测到 401 后直接 `window.location.reload()`，用户当前未保存的输入（如 AI 对话框中的长文本）会丢失，且整个 SPA 状态被重置。
- **涉及文件**：`frontend/src/utils/request.js`
- **代码位置**：第 37-42 行
- **建议修复**：改为通过 `router.push('/login')` 跳转，并保留 `redirect` 查询参数，登录后回跳。

---

## 【中等-09】WebSocket URL 硬编码端口 8000，生产环境无法使用

- **问题描述**：`frontend/src/api/websocket.js` 第 5 行 `const WS_BASE = ws://${window.location.hostname}:8000`，硬编码了后端端口。在 Docker 部署（nginx 反代到 80）或生产环境（HTTPS/WSS）下，该地址无法连接。同时未使用 `wss://` 协议，HTTPS 页面会混合内容被拦截。
- **涉及文件**：`frontend/src/api/websocket.js`
- **代码位置**：第 5 行
- **建议修复**：`const WS_BASE = ${window.location.protocol === 'https:' ? 'wss' : 'ws'}://${window.location.hostname}${window.location.port ? ':' + window.location.port : ''}`，并通过 nginx 反代 `/ws` 路径。

---

## 【中等-10】App.vue 残留多处 `console.log` 调试输出

- **问题描述**：`App.vue` 第 232、381、396 行有 `console.log` / `console.error` 调试输出，在生产环境会污染浏览器控制台，且暴露内部逻辑。
- **涉及文件**：`frontend/src/App.vue`
- **代码位置**：第 232、381、396 行
- **建议修复**：移除调试日志，或使用 `import.meta.env.DEV` 条件输出；项目已配置 ESLint，可加 `no-console` 规则。

---

## 【中等-11】vite.config.js 缺少构建优化配置

- **问题描述**：Vite 配置未设置 `build.rollupOptions.output.manualChunks`、`build.chunkSizeWarningLimit`、`build.minify` 等。当前构建产物中 `three.js`、`echarts`、`element-plus` 全部打入单一 chunk，首屏加载缓慢。`dist/index-DbmMq_hP.js` 主包体积过大。
- **涉及文件**：`frontend/vite.config.js`
- **代码位置**：整个文件未配置 `build` 字段
- **建议修复**：
  ```js
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'vendor-vue': ['vue', 'vue-router'],
          'vendor-echarts': ['echarts'],
          'vendor-three': ['three', '@tresjs/core'],
          'vendor-element': ['element-plus', '@element-plus/icons-vue']
        }
      }
    },
    chunkSizeWarningLimit: 1000
  }
  ```

---

## 【中等-12】docker-compose 未设置容器资源限制

- **问题描述**：`docker-compose.yml` 中 `backend` 和 `frontend` 服务均未配置 `mem_limit`、`cpus`、`pids_limit`。单容器异常（如内存泄漏、fork 炸弹）可能耗尽宿主机资源，影响其他服务。
- **涉及文件**：`docker-compose.yml`
- **代码位置**：第 30-75 行
- **建议修复**：
  ```yaml
  backend:
    mem_limit: 2g
    cpus: '2.0'
    pids_limit: 200
  ```

---

## 【中等-13】frontend Dockerfile 未配置 `.dockerignore`，可能将 `node_modules` 拷入镜像

- **问题描述**：`frontend/Dockerfile` 执行 `COPY . .` 时，若未配置 `.dockerignore`，会将本地 `node_modules/`、`dist/`、`.git/` 全部拷入构建上下文，严重拖慢构建并增大镜像体积。
- **涉及文件**：`frontend/Dockerfile`
- **代码位置**：第 14 行 `COPY . .`
- **建议修复**：在 `frontend/` 目录下创建 `.dockerignore`，包含 `node_modules`、`dist`、`.git`、`*.log`。

---

## 【中等-14】alembic env.py 中 `target_metadata = None`，autogenerate 完全失效

- **问题描述**：Alembic 迁移环境配置中 `target_metadata = None`，导致 `alembic revision --autogenerate` 无法自动检测模型变化生成迁移脚本，所有迁移必须手写。同时第 26 行有一处无意义的 `.replace("postgresql+psycopg2", "postgresql+psycopg2")`（自己替换自己）。
- **涉及文件**：`backend/alembic/env.py`
- **代码位置**：第 26 行、第 40 行
- **建议修复**：引入 SQLAlchemy 模型 Base 并设置 `target_metadata = Base.metadata`；删除无效 replace。

---

## 【中等-15】requirements.txt 中 `httpx` 重复声明

- **问题描述**：`httpx==0.27.2` 在第 9 行和第 23 行各出现一次，重复声明。虽不影响安装，但显得依赖管理混乱。
- **涉及文件**：`backend/requirements.txt`
- **代码位置**：第 9 行、第 23 行
- **建议修复**：删除第 23 行的重复项。

---

## 【中等-16】测试中硬编码管理员密码 `admin123`

- **问题描述**：`test_core_api.py` 第 23、36 行硬编码了登录密码 `admin123`。若 `.env` 中配置了不同的管理员密码（生产环境强制要求），所有依赖 `auth_token` fixture 的测试会失败。测试与配置耦合。
- **涉及文件**：`backend/tests/test_core_api.py`
- **代码位置**：第 23、36 行
- **建议修复**：从环境变量读取 `TEST_ADMIN_PASSWORD`，或单独创建测试用密码哈希环境。

---

## 【中等-17】conftest.py 缺少数据库隔离 fixture，测试可能污染生产数据

- **问题描述**：`conftest.py` 仅做了 `sys.path` 处理，未提供数据库事务回滚 fixture。`test_core_api.py` 中的写操作测试（如未来扩展到 `admin.py` 的知识库 CRUD）会真实写入数据库，污染数据。
- **涉及文件**：`backend/tests/conftest.py`
- **建议修复**：添加 `@pytest.fixture(autouse=True)` 包裹每个测试在事务中执行并回滚；或使用独立的测试数据库 `enterprise_building_energy_test.db`。

---

## 【中等-18】energy.py `_MODEL_CACHE` 缓存键包含小时，无上限可能内存泄漏

- **问题描述**：`_MODEL_CACHE` 的 key 格式为 `prophet_v1_YYYYMMDD_HH`，每小时产生一个新 key，每个 value 含完整 Prophet 模型对象（数十 MB）。虽然 `_cleanup_expired_cache` 会清理超过 2 倍 TTL 的项，但在高并发下仍可能短时间内积累大量模型，占用内存。
- **涉及文件**：`backend/app/api/v1/energy.py`
- **代码位置**：第 33-36 行、第 298-303 行
- **建议修复**：使用 `functools.lru_cache` 或限制 `_MODEL_CACHE` 最大条目数（如 `maxlen=3`）；考虑用 Redis 替代进程内缓存。

---

## 【中等-19】AI 工具调用历史消息中 `tool_call_id` 与 `name` 字段拼接顺序不规范

- **问题描述**：`chat.py` 第 497-502 行向 messages 追加 tool 结果时，同时包含 `tool_call_id`、`role: tool`、`name`、`content`。部分 LLM 提供商（如 OpenAI）要求 tool 消息必须严格对应 `tool_calls` 数组的顺序，且 `name` 字段在某些 API 版本中已废弃。当前实现在多工具并行调用时可能触发 400 错误。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 497-502 行
- **建议修复**：参考 OpenAI 官方文档，确保 tool 消息顺序与 `tool_calls` 严格一致；移除 `name` 字段或使用 `tool_call_id` 唯一关联。

---

## 【中等-20】admin.py 删除知识条目时 deque 重建存在并发竞态

- **问题描述**：`delete_knowledge_item` 执行 `items = list(knowledge_base)` → `items.pop(doc_index)` → `knowledge_base.clear()` → `knowledge_base.extend(items)`，四步非原子。若并发请求：A 正在 pop 索引 3，B 同时 pop 索引 5，B 的 clear 会清空 A 已读取的 list，导致数据丢失或索引错位。
- **涉及文件**：`backend/app/api/v1/admin.py`
- **代码位置**：第 325-332 行
- **建议修复**：引入 `asyncio.Lock` 或 `threading.Lock` 保护删除操作；或迁移到数据库表利用行锁。

---

## 【中等-21】前端 `App.vue` 中 `dashboardData` 通过 props 传递，但路由组件已懒加载，数据流不清晰

- **问题描述**：`App.vue` 第 220-225 行定义 `dashboardData`，第 123 行通过 `:data="dashboardData"` 传给所有 `<router-view>` 组件。但只有 `Dashboard.vue` 真正使用该 prop，其他视图（设备、AI、孪生）忽略它。这导致：① Dashboard 数据在非 Dashboard 页面也被拉取（浪费）；② 数据流不清晰，子组件无法独立刷新。
- **涉及文件**：`frontend/src/App.vue`
- **代码位置**：第 121-125 行、第 220-246 行
- **建议修复**：移除 App.vue 中的 `dashboardData` 与 `fetchDashboardData`，让 `Dashboard.vue` 在 `onMounted` 中自行拉取数据。

---

## 【中等-22】CORS 配置默认允许 `localhost:5173`，生产环境若未修改将放行任意来源

- **问题描述**：`config.py` 第 72-76 行 CORS 默认值为 `http://localhost:5173`，若生产环境未设置 `CORS_ORIGINS` 环境变量，且 `allow_credentials=True`，浏览器会拒绝跨域请求，但攻击者可通过伪造 Origin 头绕过浏览器限制（结合 SSRF）。
- **涉及文件**：`backend/app/core/config.py`
- **代码位置**：第 72-76 行
- **建议修复**：生产环境强制要求设置 `CORS_ORIGINS`，缺失时拒绝启动或仅允许同源。

---

## 【中等-23】admin.py 知识库条目 schema 不一致

- **问题描述**：`upload_doc`（chat.py 第 547-550 行）存储的 dict 使用 `filename` / `content` 键；`upload_knowledge_item`（admin.py 第 290-295 行）存储的 dict 使用 `title` / `content` / `tags` / `source` 键；`list_knowledge_base`（admin.py 第 253-269 行）读取时同时尝试 `title` 和 `filename`，逻辑混乱。这导致前端展示的知识库列表字段缺失。
- **涉及文件**：`backend/app/api/v1/chat.py` 第 547-550 行；`backend/app/api/v1/admin.py` 第 253-295 行
- **建议修复**：定义统一的 `KnowledgeDoc` Pydantic 模型（`title, filename, content, tags, source, uploaded_at`），所有写入处使用同一 schema。

---

## 【中等-24】spatial_twin.py 第 84 行对无数据建筑使用 `random.uniform` 生成假能耗

- **问题描述**：当某建筑在数据库无能耗记录时，代码用 `random.uniform(10, 50)` 生成一个假数值填充，前端无法区分真实 vs 模拟。每次刷新数值都会变化，暴露伪造行为。
- **涉及文件**：`backend/app/api/v1/spatial_twin.py`
- **代码位置**：第 84 行
  ```python
  power_val = round(random.uniform(10, 50), 1)
  ```
- **建议修复**：无数据时返回 `None` 或 `0`，并在响应中标注 `data_source: "missing"`；前端用灰色或"暂无数据"展示。

---

## 【中等-25】dashboard.py 第 79-80 行使用 `iterrows()` 性能差

- **问题描述**：`pie_df.iterrows()` 是 pandas 中性能最差的遍历方式，比 `itertuples()` 慢 5-10 倍。虽然数据量小，但与项目"向量化转换"的约定（devices.py 第 69 行注释）不一致。
- **涉及文件**：`backend/app/api/v1/dashboard.py`
- **代码位置**：第 79-80 行
- **建议修复**：改用 `pie_df.to_dict('records')` 或 `itertuples()`。

---

## 【中等-26】devices.py 第 78 行仍使用 `iterrows()`

- **问题描述**：`devices.py` 第 69 行注释明确写"向量化转换，替代 iterrows（性能提升 10-50 倍）"，但第 78 行依然使用 `for index, row in df.iterrows():`，自相矛盾。当 `size=500` 时性能尚可，但 `size=10000` 时会明显卡顿。
- **涉及文件**：`backend/app/api/v1/devices.py`
- **代码位置**：第 78-94 行
- **建议修复**：改用 `df.to_dict('records')` 后再列表推导式重构。

---

## 【中等-27】chat.py 第 276 行存在缩进错误

- **问题描述**：第 276 行 `# ====` 注释缩进为 4 空格，但上下文（第 267-275 行）的循环体缩进为 16 空格（位于 `for idx, doc in enumerate(knowledge_base):` 内）。该缩进错误虽被 Python 解释器容忍（注释行），但暴露了代码编辑过程中的格式混乱，可能掩盖真实逻辑错误。
- **涉及文件**：`backend/app/api/v1/chat.py`
- **代码位置**：第 276 行
- **建议修复**：统一缩进，运行 `black` 或 `ruff format` 格式化整个文件。

---

# 🟢 优化建议（锦上添花）

## 【优化-01】引入 Redis 替代进程内缓存

- **问题描述**：当前 `energy.py` 的 `_MODEL_CACHE`、`main.py` 的 `knowledge_base` 均为进程内变量，多 worker（uvicorn `--workers 2`）下不共享，缓存命中率低，知识库数据不一致。
- **建议**：引入 Redis 统一管理缓存与会话状态，docker-compose 中已可加一个 `redis:7-alpine` 服务。

---

## 【优化-02】添加 Prometheus 指标暴露端点

- **问题描述**：当前仅有 `/health` 与 `/readiness`，缺乏 QPS、延迟分位数、错误率等指标。
- **建议**：集成 `prometheus-fastapi-instrumentator`，自动暴露 `/metrics`，配合 Grafana 做可视化。

---

## 【优化-03】添加 CI/CD 流水线

- **问题描述**：项目无 `.github/workflows/` 或 `.gitlab-ci.yml`，代码推送后无自动化测试与构建。
- **建议**：配置 GitHub Actions：`lint → test → build docker → push registry`，并在 PR 上强制运行测试。

---

## 【优化-04】前端添加 ECharts 实例复用与 resize 防抖

- **问题描述**：`Dashboard.vue` 等视图频繁 `echarts.init`，未统一管理实例，窗口 resize 时可能多次触发 `resize()`。
- **建议**：封装 `useECharts` composable，统一实例池与 `ResizeObserver` 防抖。

---

## 【优化-05】前端添加错误边界组件

- **问题描述**：任意子组件抛错会导致整个 SPA 白屏。
- **建议**：实现 `ErrorBoundary.vue`（Vue 3 `onErrorCaptured`），包裹 `<router-view>`，展示友好错误态并提供"重试"按钮。

---

## 【优化-06】后端添加 API 版本前缀

- **问题描述**：当前路由为 `/api/login`、`/api/dashboard`，无版本前缀，未来破坏性变更无法平滑迁移。
- **建议**：统一改为 `/api/v1/login`，通过 `app.include_router(router, prefix="/api/v1")` 挂载。

---

## 【优化-07】数据库添加连接池预热与慢查询日志

- **问题描述**：SQLite 模式下每次请求新建连接，首次查询冷启动慢；无慢查询监控。
- **建议**：启动时执行 `PRAGMA warm_cache`；记录耗时 >500ms 的查询到 `logs/slow_queries.jsonl`。

---

## 【优化-08】添加数据库自动备份策略

- **问题描述**：`enterprise_building_energy.db`（498MB）无自动备份，硬件故障将丢失全部数据。
- **建议**：cron 定时 `sqlite3 .backup` 到异地；docker-compose 中加 `backup` 服务，每日打包到对象存储。

---

## 【优化-09】前端添加 PWA 离线支持

- **问题描述**：断网时整个应用不可用。
- **建议**：集成 `vite-plugin-pwa`，缓存静态资源与最近一次 Dashboard 数据，支持弱网/离线浏览。

---

## 【优化-10】添加 OpenAPI 文档增强与接口 Mock

- **问题描述**：FastAPI 默认 `/docs` 缺乏业务描述，前端联调依赖后端进度。
- **建议**：为每个端点添加 `summary`、`description`、`response_model`；使用 `openapi-typescript` 自动生成前端类型。

---

## 【优化-11】添加 LLM 调用成本监控与配额

- **问题描述**：`chat.py` 无 token 用量统计，单次 `max_tokens=16384` 可能产生高额账单。
- **建议**：记录每次调用的 `prompt_tokens` / `completion_tokens` 到数据库表 `sys_llm_usage`，按用户/天聚合，超阈值告警。

---

## 【优化-12】前端添加虚拟滚动处理大列表

- **问题描述**：`DeviceMonitor.vue` 在 `size=500` 时渲染 500 行 DOM，卡顿明显。
- **建议**：使用 `vue-virtual-scroller` 或 Element Plus `el-table-v2` 虚拟滚动。

---

## 【优化-13】后端添加请求体大小限制中间件

- **问题描述**：除 `/api/upload_doc` 外，其他 POST 接口无请求体大小限制，可被大 JSON 攻击。
- **建议**：添加中间件 `Content-Length > 1MB` 时返回 413。

---

## 【优化-14】日志添加请求体脱敏

- **问题描述**：`middleware.py` 记录请求日志，但未脱敏，密码、token 可能进入日志。
- **建议**：对 `/api/login` 等敏感路径的请求体做字段级脱敏（`password → "***"`）。

---

## 【优化-15】添加 WebSocket 心跳保活

- **问题描述**：`websocket.py` 服务端每 3 秒推送快照，但无 ping/pong 心跳，NAT/代理可能在 60 秒空闲后断开连接，客户端 `onclose` 后重连，造成连接风暴。
- **建议**：服务端每 30 秒发送 `{"type":"ping"}`，客户端响应 `{"type":"pong"}`，超时 60 秒主动断开。

---

## 【优化-16】前端 Login.vue 物理引擎在移动端性能消耗过大

- **问题描述**：`Login.vue` 的 `RobotPhysics` 引擎使用 `requestAnimationFrame` 持续运算，移动端 CPU 占用高、耗电快。
- **建议**：检测 `prefers-reduced-motion` 或设备性能，移动端禁用物理引擎，仅展示静态机器人。

---

## 【优化-17】添加 API 限流维度细化

- **问题描述**：当前限流仅基于 IP（`get_remote_address`），NAT 后多用户共享 IP 会互相影响。
- **建议**：登录后接口按 `user` 维度限流；未登录接口保留 IP 维度并提高阈值。

---

## 【优化-18】添加 LLM 输出内容安全审计

- **问题描述**：AI 生成的回复直接 SSE 推送给用户，未做内容安全审核（涉政、暴恐、隐私）。
- **建议**：集成 `baidu_aip` 或 `openai moderation` API，对输出做二级审核，命中则替换为安全提示并记录审计日志。

---

# 📎 附录：审查文件清单

## 后端文件（已审查）
| 文件路径 | 行数 | 审查重点 |
|---------|------|---------|
| `backend/app/main.py` | 125 | 入口、CORS、异常处理、调度器 |
| `backend/app/core/config.py` | 80 | 配置中心、JWT、密码哈希 |
| `backend/app/core/database.py` | 115 | 连接池、重试、SQLite/PG 双模 |
| `backend/app/core/security.py` | 83 | JWT 签发/校验、bcrypt、鉴权依赖 |
| `backend/app/core/middleware.py` | 79 | trace_id 链路追踪 |
| `backend/app/core/logging_config.py` | 113 | JSON 日志、按天轮转 |
| `backend/app/core/rate_limit.py` | 18 | slowapi 限流配置 |
| `backend/app/api/v1/chat.py` | 554 | AI 对话、工具调用、SSE、文件上传 |
| `backend/app/api/v1/login.py` | 20 | 登录路由 |
| `backend/app/api/v1/dashboard.py` | 189 | 仪表盘、COP 趋势、能耗分布 |
| `backend/app/api/v1/devices.py` | 139 | 设备列表、预测性维护 |
| `backend/app/api/v1/energy.py` | 304 | Prophet 预测、缓存、降级 |
| `backend/app/api/v1/admin.py` | 351 | 管理后台、审计日志、知识库 CRUD |
| `backend/app/api/v1/spatial_twin.py` | 100+ | 空间孪生、3D 数据 |
| `backend/app/api/v1/health.py` | 42 | 健康检查 |
| `backend/app/api/v1/websocket.py` | 131 | 实时推送 |
| `backend/app/services/ai_service.py` | 281 | AI 工具函数、SQL 沙箱 |
| `backend/app/services/ragflow_service.py` | 102 | 知识库检索 |
| `backend/app/services/sql_service.py` | 38 | SQL 校验（未使用） |
| `backend/app/services/email_service.py` | 112 | 邮件日报 |
| `backend/alembic/env.py` | 80 | 迁移环境 |
| `backend/requirements.txt` | 24 | 依赖管理 |
| `backend/Dockerfile` | 32 | 容器构建 |
| `backend/tests/test_core_api.py` | 151 | 集成测试 |
| `backend/tests/test_sql_sandbox.py` | 102 | 沙箱安全测试 |

## 前端文件（已审查）
| 文件路径 | 行数 | 审查重点 |
|---------|------|---------|
| `frontend/src/App.vue` | 455 | 主布局、全局 AI、登录态 |
| `frontend/src/router/index.js` | 82 | 路由守卫 |
| `frontend/src/api/index.js` | 289 | API 封装层 |
| `frontend/src/api/websocket.js` | 110 | WS 封装 |
| `frontend/src/utils/request.js` | 55 | fetch 工具 |
| `frontend/src/views/Dashboard.vue` | 120+ | 仪表盘视图 |
| `frontend/src/views/AiAgent.vue` | 80+ | AI 对话视图 |
| `frontend/src/views/Login.vue` | 500+ | 登录页物理引擎 |
| `frontend/src/views/AdminDashboard.vue` | 200+ | 管理后台 |
| `frontend/src/views/SpatialTwin.vue` | 150+ | 空间孪生 |
| `frontend/vite.config.js` | 28 | 构建配置 |
| `frontend/package.json` | 44 | 依赖管理 |
| `frontend/Dockerfile` | 27 | 容器构建 |

## 部署配置（已审查）
| 文件路径 | 审查重点 |
|---------|---------|
| `docker-compose.yml` | 服务编排、健康检查、资源限制 |
| `.gitignore` | 敏感文件排除 |
| `backend/.env.example` | 环境变量模板 |

---

## 🎯 修复优先级建议

### P0（本周内修复，影响功能正确性与安全）
1. 【严重-01】knowledge_base globals 检查 Bug → 核心功能失效
2. 【严重-06】文件上传无校验 → 安全漏洞
3. 【严重-08】默认密码哈希 → 启动安全
4. 【严重-10】docker-compose 默认密码 → 生产安全
5. 【严重-11】Docker root 用户 → 容器逃逸
6. 【严重-09】RagFlow prompt 注入 → AI 安全

### P1（两周内修复，影响数据真实性）
1. 【严重-02】dashboard Mock 兜底
2. 【严重-03】cop_trend 伪造数据
3. 【严重-04】predictive_maintenance 伪造数据
4. 【严重-05】predict_device_rul 随机特征
5. 【严重-07】knowledge_base 跨用户共享

### P2（下个迭代，提升质量）
- 所有中等问题中的日志规范、性能优化、测试覆盖

### P3（持续优化）
- 所有优化建议

---

> **审查人**：Trae AI Agent
> **审查耗时**：约 30 分钟（覆盖 35+ 文件）
> **报告生成时间**：2026-07-28
> **注意事项**：本报告仅做审查，未修改任何代码。修复时请逐项验证并补充对应单元测试。
