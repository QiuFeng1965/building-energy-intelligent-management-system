# 擎翼数字中枢 — 项目结构分层重构报告

> 生成时间：2026-07-28
> 重构范围：前后端代码结构分层、文件规整、README 声明

---

## 一、重构目标

将原先「单文件堆砌」的代码结构重构为「分层分目录」的标准工程架构，提升可维护性和可读性，同时确保所有功能正常运行。

---

## 二、后端重构

### 2.1 重构前

```
backend/
├── main.py          # 2024 行，所有路由+业务逻辑全堆在一个文件
├── db.py            # 数据库连接
├── auth.py          # 鉴权
├── mcp_server.py    # MCP 服务器
├── *.py             # 6 个脚本文件散落在根目录
├── *.db             # 数据库文件散落在根目录
├── *.pkl            # 模型文件散落在根目录
└── *.png            # 图表文件散落在根目录
```

### 2.2 重构后

```
backend/
├── app/                        # 应用主包（分层架构）
│   ├── main.py                 # 入口：FastAPI 创建、CORS、异常处理、路由挂载
│   ├── core/                   # 核心层
│   │   ├── config.py           # 统一配置中心（所有路径/密钥/常量集中管理）
│   │   ├── database.py         # 数据库连接（WAL + 重试 + 线程池）
│   │   └── security.py         # JWT + bcrypt 鉴权
│   ├── models/schemas.py       # Pydantic 数据模型
│   ├── services/               # 业务逻辑层
│   │   ├── ai_service.py       # LLM 客户端 + 6 个 AI 工具函数
│   │   ├── sql_service.py      # SQL 校验（sqlglot）
│   │   ├── ragflow_service.py  # 知识库检索
│   │   └── email_service.py    # 定时邮件日报
│   ├── api/v1/                 # 路由层（9 个模块）
│   │   ├── login.py            # POST /api/login
│   │   ├── dashboard.py        # /api/dashboard, /api/cop_trend, /api/energy_distribution
│   │   ├── devices.py          # /api/devices, /api/equipment/predictive_maintenance
│   │   ├── spatial_twin.py     # /api/spatial-twin/*, /api/buildings/{id}/3d-data
│   │   ├── chat.py             # /api/chat/stream, /api/upload_doc
│   │   ├── energy.py           # /api/energy/forecast
│   │   ├── report.py           # /api/report/weekly_ai
│   │   ├── admin.py            # /api/admin/dashboard
│   │   └── websocket.py        # /ws/realtime_energy
│   └── utils/name_maps.py      # 名称映射字典
├── scripts/                    # 6 个独立脚本
├── mcp/                        # MCP 服务器
├── data/                       # 数据库 + 模型文件
├── assets/                     # 图表 PNG
├── logs/                       # 运行日志
├── temp/                       # 临时文件
├── .env / .env.example
└── main.py / db.py / auth.py   # [已弃用] 旧版备份
```

### 2.3 关键改进

| 改进点 | 说明 |
|--------|------|
| **配置中心化** | `config.py` 统一管理所有路径、密钥、常量，基于 `__file__` 推导绝对路径，不受工作目录影响 |
| **路由模块化** | 2024 行 main.py 拆分为 9 个路由模块，每个模块用 `APIRouter()` 独立管理 |
| **业务逻辑分离** | AI、SQL、知识库、邮件服务提取到 `services/` 层，路由层只负责 HTTP 收发 |
| **数据文件隔离** | `.db`、`.pkl` 移入 `data/` 目录，与代码分离 |
| **脚本独立** | 6 个一次性脚本移入 `scripts/`，不与运行时代码混放 |

---

## 三、前端重构

### 3.1 重构前

```
src/
├── components/    # 3D 组件 + BuildingLibrary.js 混在一起
├── views/         # 页面
├── composables/   # 组合式函数
├── utils/         # 工具函数
└── ...            # 无统一 API 调用层
```

### 3.2 重构后

```
src/
├── api/               # 新增：统一 API 调用层（12 个函数）
│   └── index.js
├── config/            # 新增：配置/常量目录
│   └── BuildingLibrary.js    # 从 components/ 迁入
├── components/        # 仅保留 .vue 组件
├── views/             # 页面视图
├── composables/       # 组合式函数
├── utils/             # 工具函数
└── ...
```

### 3.3 关键改进

| 改进点 | 说明 |
|--------|------|
| **统一 API 层** | 新增 `api/index.js`，导出 12 个 API 函数，基于 `utils/request.js` 的 `safeFetch` |
| **配置分离** | `BuildingLibrary.js` 从 `components/` 迁入 `config/`，与 .vue 组件分离 |
| **import 路径更新** | `BuildingManager.vue` 中的 import 路径已同步更新 |

---

## 四、README 声明

为每个目录创建了 README.md 声明文件（共 20+ 个），覆盖：
- 根目录总览
- 后端 app/ 各子目录
- 前端 src/ 各子目录
- 7 个业务文档文件夹

---

## 五、冒烟测试结果

| # | 接口 | 状态码 | 结果 |
|---|------|--------|------|
| 1 | POST /api/login | 200 | ✅ |
| 2 | GET /api/dashboard | 200 | ✅ |
| 3 | GET /api/cop_trend | 200 | ✅ |
| 4 | GET /api/spatial-twin/campus-data | 200 | ✅ |
| 5 | GET /api/devices | 200 | ✅ |
| 6 | GET http://localhost:5173 | 200 | ✅ |

---

## 六、启动方式

```bash
# 后端（新分层架构）
cd backend
uvicorn app.main:app --host 0.0.0.0 --port 8000

# 前端
cd frontend
npm run dev
```

登录账号：`admin` / `admin123`
