# 上线前安全审计与深色模式修复报告

**生成时间**：2026-07-28
**项目**：Building Energy Intelligent Management System2（擎翼数字中枢）
**审计范围**：深色模式配色、上线前漏洞扫描与修复

---

## 一、深色模式修复

### 问题
深色模式下整体颜色不搭，主要表现为：
- 未引入 Element Plus 官方深色 CSS 变量
- 自定义覆盖不完整，半透明白底在深色下刺眼
- 部分组件（dialog / dropdown / select / 登录页自动填充）未适配

### 修复
| 文件 | 修复内容 |
|---|---|
| `frontend/src/main.js` | 引入 `element-plus/theme-chalk/dark/css-vars.css`，启用 Element Plus 官方深色变量 |
| `frontend/src/style.css` | 补充 `bg-white/80`、`bg-white/60` 等透明度变体的深色覆盖；新增 dialog / drawer / dropdown / select / popover / 登录页自动填充等组件样式覆盖 |

---

## 二、漏洞扫描与修复

### 严重漏洞（已修复 2 项）

#### V1：敏感文件可能打入 Docker 镜像
- **风险**：`.env`、`*.pem`、`*.key`、`data/*.db`、`logs/` 等敏感文件可能被复制进镜像，造成密钥和数据库泄露
- **修复**：在 [backend/.dockerignore](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/.dockerignore) 中追加过滤规则，覆盖 `.env`、`*.pem`、`*.key`、`data/`、`logs/`、`*.db` 等

#### V2：admin123 弱口令兜底
- **风险**：生产环境缺失 `ADMIN_PASSWORD_HASH` 时使用默认弱口令 `admin123`，可被暴力破解
- **修复**：在 [backend/app/core/config.py](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/app/core/config.py) 中校验 `ENV=production` 时强制要求 `ADMIN_PASSWORD_HASH`，缺失则 `RuntimeError` 终止启动

### 高危漏洞（已修复 5 项）

#### V3：WebSocket 接口无鉴权
- **风险**：`/ws/realtime_energy` 未校验 token，可被未授权访问读取实时数据
- **修复**：
  - 后端 [websocket.py](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/app/api/v1/websocket.py)：从 query 参数读取 token 并校验，无效则关闭连接（code=4401）
  - 前端 [websocket.js](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/frontend/src/api/websocket.js) 及 [EnergyAnalysis.vue](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/frontend/src/views/EnergyAnalysis.vue)：连接时附加 token 参数

#### V4：CORS 配置不安全
- **风险**：生产环境可能配置 `CORS_ORIGINS=*`，与 `allow_credentials=True` 冲突，导致凭证泄露
- **修复**：在 [config.py](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/app/core/config.py) 中校验生产环境禁止通配符 origin，违规则 `RuntimeError` 终止启动

#### V5：异常信息泄露
- **风险**：50+ 处 `except Exception` 块将 `str(e)` 直接返回给客户端，泄露数据库字段名、SQL 错误、内部堆栈等敏感信息
- **修复**：
  - 新增统一异常处理工具 [route_error.py](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/app/core/route_error.py)，提供 `handle_route_error()` 和 `safe_error_dict()` 函数
  - 批量替换 70+ 处异常响应，详细堆栈仅写入日志，客户端只收到通用提示
  - 覆盖文件：admin.py / dashboard.py / devices.py / anomaly.py / ar.py / carbon.py / benchmark.py / rul.py / vpp.py / twin3d.py / multi_energy.py / push_service.py / workorder_pro.py / alert_center.py / audit_report.py / spatial_twin.py / observability.py / chat.py / agents.py / roi_calculator.py / workorders.py / export.py / ai_service.py / ragflow_service.py / sql_service.py / report_service.py

#### V6：edge.py 全局状态并发不安全
- **风险**：`_gateway_state` 字典的 `messages_sent += 1` 计数器在多线程并发下丢失更新；`anomaly_injection` 的读写存在竞态；自动清除任务可能误清新注入
- **修复**：在 [edge.py](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/app/api/v1/edge.py) 中引入 `threading.Lock` 保护全局状态：
  - 提供 `_get_state_snapshot()` / `_get_anomaly_injection()` 线程安全快照接口
  - `gateway_status` 加锁更新计数器
  - `inject_anomaly` 加锁写入，自动清除任务通过对象引用比较避免误清

#### V7：CSP 策略宽松
- **风险**：开发环境的 `'unsafe-eval'`、`http://localhost:*`、`ws:` 等指令在生产环境保留，扩大 XSS 攻击面
- **修复**：在 [middleware.py](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/app/core/middleware.py) 中区分环境：
  - 生产环境：移除 `unsafe-eval`，`connect-src` 收紧为 `wss:` + 已知可信域名
  - 开发环境：保留原宽松策略以支持 Vite HMR

### 中低危漏洞（已修复 1 项，已知遗留 1 项）

#### V8：前端路由守卫未校验 token 过期
- **风险**：原路由守卫仅检查 token 是否存在，过期 token 仍可访问受保护页面，直到首次 API 401 才被踢出
- **修复**：在 [router/index.js](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/frontend/src/router/index.js) 中新增 `isTokenValid()` 函数：
  - 解析 JWT payload 的 `exp` 字段，过期则清除 token 并跳转登录
  - 已登录访问登录页时也校验有效性

#### V9（已知遗留）：JWT 无撤销机制
- **风险**：JWT 一旦签发在有效期内始终有效，无法主动登出或吊销
- **状态**：未修复。建议后续引入 Redis 黑名单或短期 access_token + 长期 refresh_token 方案
- **缓解**：当前 token 有效期较短（参考 [config.py](file:///c:/Users/Administrator/Desktop/🔧%20开发项目/Building%20Energy%20Intelligent%20Management%20System2/backend/app/core/config.py)），路由守卫已增加过期校验

#### V10（已知遗留）：token 存储在 localStorage
- **风险**：localStorage 中的 token 可被 XSS 攻击窃取
- **状态**：未修复。迁移到 HttpOnly Cookie 需重构前后端认证流程，影响范围大
- **缓解**：CSP 已收紧减少 XSS 风险；建议后续独立排期迁移

---

## 三、修复统计

| 类别 | 严重 | 高危 | 中低危 | 合计 |
|---|---|---|---|---|
| 已修复 | 2 | 5 | 1 | **8** |
| 已知遗留 | 0 | 0 | 2 | 2 |

| 修改文件类型 | 数量 |
|---|---|
| 后端 Python 文件 | 28 |
| 后端配置文件 | 2（`.dockerignore`、`config.py`） |
| 前端 JS/Vue 文件 | 4（`main.js`、`style.css`、`router/index.js`、`websocket.js`） |
| **合计** | **34** |

### 验证结果
- ✅ 27 个修改的 Python 文件通过 `ast.parse` 语法检查
- ✅ Grep 验证：所有响应给客户端的 `message` / `reason` / `detail` / `reply` 字段中已无 `str(e)` 或 `f"{e}"` 残留
- ✅ 剩余 60 处 `logger.exception(f"...: {e}")` 全部为日志记录（合规，不返回客户端）
- ✅ 全局异常处理器（main.py）已正确屏蔽 DBUnavailableError 和 Exception 的详情

---

## 四、生产部署前必做检查清单

1. **环境变量**：
   - `ENV=production`
   - `JWT_SECRET` 已设置（缺失即 `RuntimeError`）
   - `ADMIN_PASSWORD_HASH` 已设置（bcrypt 哈希）
   - `CORS_ORIGINS` 已设置为具体域名（禁止 `*`）
2. **Docker 镜像**：构建后检查镜像内不含 `.env`、`*.db`、`*.pem` 等敏感文件
3. **HTTPS**：必须通过 HTTPS 访问，HSTS 和 CSP `wss:` 才会生效
4. **依赖**：建议运行 `pip audit` 或 `npm audit` 检查第三方依赖漏洞

---

## 五、已知遗留与后续建议

| 编号 | 项目 | 优先级 | 建议 |
|---|---|---|---|
| V9 | JWT 撤销机制 | 中 | 引入 Redis 黑名单或 refresh_token 方案 |
| V10 | token 迁移到 HttpOnly Cookie | 中 | 独立排期重构认证流程 |
| - | 第三方依赖 CVE 扫描 | 中 | 接入 `pip-audit` / `npm audit` CI 流水线 |
| - | 接口限流审计 | 低 | 复核各路由 `@limiter.limit` 阈值是否符合业务峰值 |
