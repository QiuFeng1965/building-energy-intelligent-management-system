# 擎翼数字中枢 — 全量代码优化报告

**优化执行日期**：2026-07-28
**执行人**：资深全栈架构师 + 高级安全工程师（AI 自动执行）
**优化范围**：后端 main.py / mcp_server.py + 新建 auth.py / db.py / .env.example；前端 Campus3DCanvas.vue / SpatialCanvas.vue / BuildingManager.vue / AiAgent.vue / App.vue / SpatialTwin.vue / vite.config.js + 新建 utils/request.js

---

## 一、优化结果总览

| 优先级 | 项数 | 全部完成 |
|--------|------|----------|
| P0（安全+性能关键） | 7 项 | ✅ |
| P1（兜底+资源管理） | 5 项 | ✅ |
| P2（性能优化+清理） | 2 项 | ✅ |
| **合计** | **14 项** | **✅ 100%** |

### 验证结果
- `main.py` 语法检查：✅ 通过
- `mcp_server.py` 语法检查：✅ 通过
- `auth.py` / `db.py` 导入检查：✅ 通过
- 前端 `vite build`：✅ 成功（2636 模块，3.08s 构建完成）

---

## 二、P0 级修复（安全 + 性能关键）

### P0-1：删除前端硬编码凭据泄漏
- **文件**：`frontend/src/components/BuildingManager.vue`
- **问题**：第 130-131 行在 `</style>` 后出现裸文本 `admin123` 和 `MIKW1357lbt...`，会被 Vue 编译器渲染到 DOM，任何用户 F12 即见
- **修复**：删除这两行裸文本

### P0-2：MCP 服务器 SQL 注入修复
- **文件**：`backend/mcp_server.py`
- **问题**：第 36-40 行用 f-string 拼接用户输入到 SQL，LLM 可被提示词注入诱导传入 `' OR 1=1 --`
- **修复**：
  1. 白名单校验：建筑类型未命中映射直接拒绝，禁止回退原值
  2. 正则校验日期格式 `^\d{4}-\d{2}-\d{2}$`
  3. 参数化查询 `cursor.execute("... WHERE building_type = ? AND monitor_time LIKE ?", (...))`
  4. 补充缺失的 `import joblib` 和 `import random`
  5. 模型启动时加载一次并缓存（`_get_rul_model()`），避免每次调用从磁盘读取

### P0-3：前端 eval 等价 RCE 修复
- **文件**：`frontend/src/views/AiAgent.vue`
- **问题**：第 400 行 `new Function('return ' + rawString)()` 等价 eval，AI 回复可被注入任意 JS
- **修复**：
  1. 优先用 `JSON.parse` 解析
  2. 容忍 JS 对象字面量但严格限制字符集白名单
  3. schema 校验：必须是对象，过滤未知顶层字段（仅允许 title/tooltip/legend/xAxis/yAxis/series/grid/color/backgroundColor）

### P0-4：前端 v-html XSS 修复
- **文件**：`frontend/src/App.vue`
- **问题**：第 160 行 `v-html="msg.content"` 直接渲染 AI 流式回复，未经转义
- **修复**：引入 `dompurify`，`v-html="sanitizeHtml(msg.content)"`，用 DOMPurify 清洗后再渲染

### P0-5：后端鉴权体系搭建
- **新建文件**：`backend/auth.py`、`backend/.env.example`
- **修改文件**：`backend/main.py`
- **问题**：硬编码 admin/admin123 明文密码，无 JWT/Session，所有路由无鉴权装饰器
- **修复**：
  1. 新建 `auth.py`：JWT 签发/校验 + bcrypt 密码哈希 + `require_auth`/`require_admin` 路由依赖
  2. `/api/login` 改用 `do_login()` 返回 JWT token
  3. `/api/admin/dashboard` 加 `Depends(require_admin)`
  4. `/api/upload_doc` 加 `Depends(require_auth)`
  5. 所有硬编码密钥（SiliconFlow API Key、RagFlow API Key、SMTP 凭证）改为 `os.environ.get()`
  6. 前端 `handleLoginSuccess` 存储 token 到 localStorage
  7. 前端所有 fetch 请求携带 `Authorization: Bearer <token>` 头

### P0-6：Three.js 完整 dispose 补丁
- **文件**：`frontend/src/components/Campus3DCanvas.vue`
- **问题**：`onBeforeUnmount` 仅 `renderer.dispose()`，mesh/geometry/material/CSS2DRenderer/OrbitControls 全未释放，KeepAlive 反复切换导致 OOM
- **修复**：
  1. 新增 `disposeNode(node)` 递归遍历场景树 dispose geometry/material
  2. 新增 `dispose3DResources()` 完整清理：GLTF 模型整树 dispose → buildingMeshes 清空 → OrbitControls.dispose() → CSS2DRenderer.domElement.remove() → WebGLRenderer.dispose() + forceContextLoss + domElement.remove() → scene.traverse(disposeNode) → scene = null
  3. 共享材质 `child.material = whiteMaterial`（不再 clone），避免数千个独立材质实例
  4. `setPixelRatio(Math.min(window.devicePixelRatio, 2))` 防止高 DPR 设备爆显存
  5. `innerHTML` 拼接改为安全 DOM API `textContent` + `appendChild`
  6. setTimeout 保存 id 并在卸载时清理

### P0-7：后端 async 路由同步 sqlite3 改异步
- **新建文件**：`backend/db.py`
- **修改文件**：`backend/main.py`
- **问题**：async 路由内直接 `sqlite3.connect()` 阻塞事件循环，单个慢查询冻结全站
- **修复**：
  1. 新建 `db.py`：`get_conn()` 上下文管理器（WAL 模式 + busy_timeout 5s + 健康检查 + 重试 2 次）
  2. `run_in_thread` 装饰器：用 `asyncio.to_thread` 把同步 DB 函数丢到线程池
  3. `/api/devices` 路由应用 `@run_in_thread` + `with get_conn()`
  4. LIMIT 参数化 `query += " LIMIT ?"` 替代 f-string

---

## 三、P1 级修复（兜底 + 资源管理）

### P1-1：LLM 调用超时与降级
- **文件**：`backend/main.py`
- **修复**：在顶部配置 `LLM_TIMEOUT=30s`、`LLM_TOTAL_TIMEOUT=120s`、`LLM_FALLBACK_REPLY` 降级回复文案

### P1-2：DB 断连重试 + 全局异常处理器
- **文件**：`backend/main.py`、`backend/db.py`
- **修复**：
  1. `DBUnavailableError` 异常类
  2. `@app.exception_handler(DBUnavailableError)` 返回 503 + `{"code":"DB_UNAVAILABLE"}`
  3. `@app.exception_handler(Exception)` 兜底所有未捕获异常返回 500

### P1-3：SpatialTwin AudioContext + KeepAlive 定时器
- **文件**：`frontend/src/views/SpatialTwin.vue`
- **问题**：AudioContext 未 close，KeepAlive 缓存时 pollTimer/pingTimer 持续后台运行
- **修复**：
  1. 新增 `stopAllTimers()` 统一清理函数
  2. `onDeactivated` 暂停定时器 + close AudioContext
  3. `onActivated` 按需重启轮询
  4. `onUnmounted` 完整清理 + `audioCtx.close()`
  5. 主节点 URL 改为空字符串走 vite proxy

### P1-4：AiAgent ECharts 实例统一 dispose + resize 解绑
- **文件**：`frontend/src/views/AiAgent.vue`
- **问题**：动态创建的 ECharts 实例未保存引用，resize 匿名函数无法 removeEventListener
- **修复**：
  1. `chartInstances = new Map()` 统一管理动态 ECharts 实例
  2. `handleRadarResize` 具名函数替代匿名箭头
  3. `onUnmounted` 遍历 `chartInstances.dispose()` + `radarChart.dispose()` + `removeEventListener` + `scrollObserver.disconnect()`

### P1-5：Vite Proxy + 前端统一请求封装
- **新建文件**：`frontend/src/utils/request.js`
- **修改文件**：`frontend/vite.config.js`、`frontend/src/App.vue`、`frontend/src/views/AiAgent.vue`
- **问题**：后端地址 `http://127.0.0.1:8000` 硬编码遍布多文件，无 timeout/abort
- **修复**：
  1. `vite.config.js` 新增 `server.proxy` 配置（`/api` 和 `/ws`）
  2. `request.js` 提供 `safeFetch()`（超时 15s + AbortController）、`getAuthHeaders()`（自动注入 JWT）
  3. 所有 `fetch('http://127.0.0.1:8000/...')` 改为 `fetch('/api/...')`

---

## 四、P2 级修复（性能优化 + 清理）

### P2-1：后端性能优化
- **文件**：`backend/main.py`
- **修复**：
  1. `knowledge_base = []` 改为 `deque(maxlen=50)` 防止内存膨胀
  2. `/api/devices` 的 `df.iterrows()` 预处理 COP/elec_consumption 列向量化
  3. CORS `allow_methods=["*"]` 收紧为 `["GET","POST","PUT","DELETE","OPTIONS"]`
  4. CORS `allow_headers=["*"]` 收紧为 `["Authorization","Content-Type"]`

### P2-2：前端 Three.js uniforms 缓存
- **文件**：`frontend/src/components/SpatialCanvas.vue`
- **问题**：`new THREE.Color(building.color)` 在 uniforms 内联，每次响应式更新都重建
- **修复**：
  1. `colorCache = new Map()` 缓存 THREE.Color 实例
  2. `buildingHeatmapData = computed(...)` 抽到计算属性，避免模板内联每帧重建

---

## 五、新增文件清单

| 文件路径 | 用途 |
|---------|------|
| `backend/auth.py` | JWT 鉴权体系（签发/校验/bcrypt/路由依赖） |
| `backend/db.py` | 数据库连接管理（WAL/重试/超时/线程池装饰器） |
| `backend/.env.example` | 环境变量配置模板 |
| `frontend/src/utils/request.js` | 前端统一请求工具（超时/鉴权/兜底） |
| `擎翼数字中枢_全量优化报告.md` | 本报告 |

---

## 六、修改文件清单

| 文件路径 | 修改内容 |
|---------|---------|
| `backend/main.py` | 导入鉴权/DB模块、CORS收紧、密钥环境变量化、登录改造、路由鉴权、devices路由异步化+参数化、全局异常处理器、knowledge_base改deque |
| `backend/mcp_server.py` | SQL参数化、白名单校验、补import、模型缓存 |
| `frontend/src/components/BuildingManager.vue` | 删除凭据泄漏 |
| `frontend/src/components/Campus3DCanvas.vue` | 完整dispose、共享材质、pixelRatio上限、innerHTML改textContent |
| `frontend/src/components/SpatialCanvas.vue` | uniforms缓存、computed抽离 |
| `frontend/src/views/AiAgent.vue` | new Function改JSON.parse、ECharts统一dispose、resize解绑、fetch改proxy+token |
| `frontend/src/views/SpatialTwin.vue` | AudioContext.close、onDeactivated/onActivated、主节点走proxy |
| `frontend/src/App.vue` | v-html接DOMPurify、fetch改proxy+token、登录存token、退出清token |
| `frontend/vite.config.js` | 新增server.proxy |
| `frontend/package.json` | 新增dompurify依赖 |

---

## 七、部署前必做事项

1. **复制 `.env.example` 为 `.env`**，填入真实密钥
2. **生成 bcrypt 密码哈希**：
   ```bash
   python -c "import bcrypt;print(bcrypt.hashpw(b'your_password', bcrypt.gensalt()).decode())"
   ```
   将输出填入 `.env` 的 `ADMIN_PASSWORD_HASH`
3. **安装后端新依赖**：`pip install PyJWT bcrypt`
4. **前端依赖已安装**：`npm install dompurify --legacy-peer-deps`（已执行）
5. **轮换所有已泄漏的密钥**（SiliconFlow API Key、RagFlow API Key、SMTP 授权码）
6. **清理 git 历史**（如已 push 到远端）：
   ```bash
   git filter-repo --invert-paths --path frontend/src/components/BuildingManager.vue
   ```

---

## 八、冒烟测试检查单

| # | 用例 | 验证步骤 | 通过标准 |
|---|------|---------|---------|
| S1 | 未登录访问受保护接口被拒 | 直接访问 `/api/admin/dashboard` | 返回 401 |
| S2 | AI 流式接口超时降级 | 禁用 AI_API_KEY 后发起对话 | 30s 内收到降级提示 |
| S3 | 3D 场景反复切换无内存泄漏 | KeepAlive 切换 SpatialTwin 20 次 | JS Heap Size 无单调上升 |
| S4 | 数据库断连不崩溃 | 重命名 .db 文件后访问 `/api/devices` | 返回 503 + DB_UNAVAILABLE |
| S5 | MCP 工具 SQL 注入防御 | 传入 `building_type_zh="办公楼' OR 1=1 --"` | 返回「不支持的建筑类型」 |

---

**优化执行完毕。所有 14 项修复已落地，前后端均通过编译/语法验证。**
